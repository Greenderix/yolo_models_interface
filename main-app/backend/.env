# DB (async SQLAlchemy / asyncpg)
DATABASE_URL=postgresql+asyncpg://dispatcher:secret123@dispatcher_postgres:5432/dispatcher_db
# дублируем на случай сборки DSN из частей:
POSTGRES_HOST=dispatcher_postgres
POSTGRES_PORT=5432
POSTGRES_DB=dispatcher_db
POSTGRES_USER=dispatcher
POSTGRES_PASSWORD=secret123

# S3 (boto3 требует http:// в endpoint_url)
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=violations
